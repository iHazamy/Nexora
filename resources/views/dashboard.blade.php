@extends('layouts.app')
@section('title','Dashboard')
@section('content')
<div class="mb-8 flex items-center justify-between"><div><p class="text-sm text-slate-500">A simple overview of your invoices.</p></div><a href="{{ route('invoices.create') }}" class="btn-primary">Create invoice</a></div>
<div class="grid gap-5 md:grid-cols-3"><div class="card"><p class="label">Total invoices</p><p class="metric">{{ $count }}</p></div><div class="card"><p class="label">Total sales</p><p class="metric">RM {{ number_format($sales,2) }}</p></div><div class="card"><p class="label">Outstanding balance</p><p class="metric">RM {{ number_format($outstanding,2) }}</p></div></div>
<div class="card mt-8 overflow-hidden p-0"><div class="flex items-center justify-between px-6 py-5"><h2 class="font-semibold">Recent invoices</h2><a class="text-sm font-medium text-indigo-600" href="{{ route('invoices.index') }}">View all</a></div><table class="w-full text-left text-sm"><thead><tr class="border-y border-slate-100 bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><th class="px-6 py-3">Invoice</th><th class="px-6 py-3">Customer</th><th class="px-6 py-3">Total</th><th class="px-6 py-3">Status</th></tr></thead><tbody>@forelse($recent as $invoice)<tr class="border-b border-slate-100"><td class="px-6 py-4 font-medium"><a href="{{ route('invoices.show',$invoice) }}">{{ $invoice->number }}</a></td><td class="px-6 py-4">{{ $invoice->customer_name }}</td><td class="px-6 py-4">RM {{ number_format($invoice->grand_total,2) }}</td><td class="px-6 py-4"><span class="badge {{ $invoice->status==='Paid'?'badge-paid':'badge-open' }}">{{ $invoice->status }}</span></td></tr>@empty<tr><td colspan="4" class="px-6 py-10 text-center text-slate-500">No invoices yet. Create your first one to get started.</td></tr>@endforelse</tbody></table></div>
@endsection
