<!doctype html><html><head><meta charset="utf-8">@include('invoices.partials.pdf-style')</head><body><div class="header"><div class="brand">@if(!empty($settings['logo_data']))<img class="logo" src="{{ $settings['logo_data'] }}">@endif<div class="company">{{ $settings['company_name'] ?? 'Your Company Name' }}</div><div class="muted">{!! nl2br(e($settings['address'] ?? '')) !!}</div><div class="muted">{{ $settings['phone'] ?? '' }} {{ !empty($settings['phone']) && !empty($settings['email']) ? ' · ' : '' }}{{ $settings['email'] ?? '' }}</div><div class="muted">{{ !empty($settings['registration_number']) ? 'Reg. No. '.$settings['registration_number'] : '' }}</div></div><div class="meta"><h1>RECEIPT</h1><strong>{{ $receiptNumber }}</strong><br><span class="muted">For invoice {{ $invoice->number }}</span></div><div class="clear"></div></div><div class="section"><table class="cols"><tr><td><div class="label">Received from</div><strong>{{ $invoice->customer_name }}</strong><br>@if($invoice->attention)Attn: {{ $invoice->attention }}<br>@endif{{ $invoice->customer_phone }}</td><td><div class="label">Payment details</div><strong>Payment date:</strong> {{ $invoice->paid_at?->format('d M Y') ?? '—' }}<br><strong>Invoice date:</strong> {{ $invoice->invoice_date->format('d M Y') }}<br>@if($invoice->bankAccount)<strong>Paid to:</strong> {{ $invoice->bankAccount->bank_name }} ({{ $invoice->bankAccount->account_number }})@endif</td></tr></table></div>@include('invoices.partials.items-table')<div class="stamp">PAID IN FULL</div><div class="footer">Thank you for your business.</div></body></html>
