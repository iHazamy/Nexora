<!doctype html><html><head><meta charset="utf-8">@include('invoices.partials.pdf-style')</head><body><div class="header"><div class="brand">@if(!empty($settings['logo_data']))<img class="logo" src="{{ $settings['logo_data'] }}">@endif<div class="company">{{ $settings['company_name'] ?? 'Your Company Name' }}</div><div class="muted">{!! nl2br(e($settings['address'] ?? '')) !!}</div><div class="muted">{{ $settings['phone'] ?? '' }} {{ !empty($settings['phone']) && !empty($settings['email']) ? ' · ' : '' }}{{ $settings['email'] ?? '' }}</div><div class="muted">{{ !empty($settings['registration_number']) ? 'Reg. No. '.$settings['registration_number'] : '' }}</div></div><div class="meta"><h1>INVOICE</h1><strong>{{ $invoice->number }}</strong></div><div class="clear"></div></div><div class="section"><table class="cols"><tr><td><div class="label">Bill to</div><strong>{{ $invoice->customer_name }}</strong><br>@if($invoice->attention)Attn: {{ $invoice->attention }}<br>@endif{{ $invoice->customer_phone }}</td><td><div class="label">Invoice details</div><strong>Invoice date:</strong> {{ $invoice->invoice_date->format('d M Y') }}<br><strong>Event date:</strong> {{ $invoice->event_date?->format('d M Y') ?: '—' }}<br><strong>Due date:</strong> {{ $invoice->due_date?->format('d M Y') ?: '—' }}</td></tr></table></div>@include('invoices.partials.items-table')@if($invoice->bankAccount)<div class="section"><div class="label">Bank details</div><strong>{{ $invoice->bankAccount->bank_name }}</strong><br>{{ $invoice->bankAccount->account_number }}@if($invoice->bankAccount->account_holder)<br>{{ $invoice->bankAccount->account_holder }}@endif</div>@endif @if(!empty($settings['terms']))<div class="section"><div class="label">Terms & conditions</div><div class="muted">{!! nl2br(e($settings['terms'])) !!}</div></div>@endif<div class="footer">Thank you for your business.</div></body></html>
